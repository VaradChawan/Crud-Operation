Steps to create node js project

1. npm init
--> Add the details (Application Name,description, author, main page (entry point-> (main.js/index.js)) )

2.  Let's install express --> npm install express --save
3. install mongodb and mongoose --> "npm install mongodb" and "npm install mongoose"
4. install nodemon --> reason to install--> "whenever we make small changes we need to restart server manually so instead of that we will use nodemon and it will handle the server"
nodemon is use for development purpose only while the project is live on production nodemon is not required.
==>For that we will use the below command-->
    npm install nodemon --save-dev
5. 